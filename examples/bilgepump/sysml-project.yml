# =============================================================================
# sysml-project.yml — SysML v2 project manifest
#
# Two keys are consumed by the tooling:
#
#   layers            — COMPLETE layer list in strict import order.
#                       Used by commit.sh (POST to API) and ci_kernel_validate.py
#                       --dry-run (file-existence check).
#                       Imported-by-nothing must come first (Library has no
#                       intra-model imports).
#
#   validation_layers — POSITIVE-TEST subset for kernel execution in CI.
#                       Used by ci_kernel_validate.py (full kernel run).
#                       Must exclude any layer whose analysis def blocks contain
#                       intentional VIOLATED assertions (negative tests).
#                       FMEA.sysml and UQ.sysml are excluded here — they are
#                       validated separately by examples/bilgepump/Safety.ipynb.
#                       Omit this key to run all layers (may produce expected failures).
#
# To adapt for a new project: change 'name', 'description', and both lists.
# =============================================================================

name: BilgePumpSystem
version: 0.0.1
description: "SysML v2 bilge pump verification — IMO MARPOL, DNV, IEC 60945, SOLAS"

# publish_root — the umbrella package name for the UNIFIED single-project publish.
# make_publish_notebook.py loads every layer below into the kernel, then emits an
# umbrella `package <publish_root> { import BilgePump_<each layer>::*; }` cell and
# a single `%publish <publish_root>`. Publishing one package to one project name
# keeps the project UUID stable across republishes (vs. one project per layer,
# where per-layer UUIDs drift). Adding a new layer below is automatically folded
# into the umbrella — no other file needs editing for the publish step.
publish_root: BilgePump

# assertions_module — optional python module (next to this manifest) exposing an
# ASSERTIONS list. `sysml publish` emits one tagged `%eval` cell per entry so the
# executed verdicts can be materialized into the `sysml_assertions` table.
assertions_module: bilgepump_assertions

# Layers — RAAML folded into Requirements.sysml (render-only provenance) and UQ
# folded into Analysis.sysml (deterministic worst-case margin probe); both are
# no longer standalone layers. Requirements now defines the RAAML stereotypes and
# is loaded before Safety/FMEA, so import order stays valid without a RAAML-first
# entry. Adding a new layer below is folded into the umbrella automatically.
layers:
  - Library.sysml
  - Architecture.sysml
  - Requirements.sysml
  - Analysis.sysml
  - Safety.sysml
  - FMEA.sysml
  - StateMachine.sysml

# Positive-test subset for kernel CI execution.
# FMEA.sysml: contains 4 negative-test analysis defs — designed to produce
#             VIOLATED assertions (FM-S-001, FM-PA-002, FM-PB-001, FM-C-001).
#             Excluded from the positive subset; syntax-validated via --dry-run.
# StateMachine.sysml: no VIOLATED assertions; included in validation.
validation_layers:
  - Library.sysml
  - Architecture.sysml
  - Requirements.sysml
  - Analysis.sysml
  - Safety.sysml
  - StateMachine.sysml

# Z3 formal analysis layer (not executed by the SysML kernel — Python only).
# Run with: python verify.py --z3
# Or standalone: python examples/bilgepump/formal_analysis.py
# Six levels of increasing complexity:
#   L1  Symbolic baseline (LRA) — REQ-001 proof by refutation
#   L2  Efficiency floor (NRA)  — discover REQ-004 vs FT-002 gap
#   L3  Accuracy envelope (LRA) — find MARPOL boundary for FT-001/FT-003
#   L4  Response window (LRA)   — critical inflow for FT-004 timing chain
#   L5  Adversarial (NRA)       — REQ-001..006 pass, FT-002 still violated
#   L6  Bounded temporal (LIA)  — override ordering in state machine (depth≤8)
z3_layers:
  - formal_analysis.py
