# SysML v2 Bilge Pump System — Python dependencies
#
# Primary verification engine: local SysML v2 Jupyter kernel (setup.sh).
# Remote SST API is optional (--publish flag in verify.py).

jupyter>=1.0.0
requests>=2.28.0         # optional: only needed for --publish
matplotlib>=3.7.0
networkx>=3.0            # diagram generation (--visual flag)
